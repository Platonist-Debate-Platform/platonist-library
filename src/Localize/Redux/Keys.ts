export enum LocalizeActionKeys {
  Reset = '@localize/resetLanguage',
  Select = '@localize/selectLanguage',
}

export enum AvailableLanguages {
  De = 'de',
  Default = 'defaults',
  En = 'en',
}

export enum AvailableLanguagesExtended {
  DeDE = 'de-De',
  En150 = 'en-150',
}